#include <string>

class Command {

  Command(std::string commandName, int num_req_args)
      : _num_req_args(num_req_args), _commandName(commandName) {}

  virtual bool execute() = 0;
  virtual bool validateArgs() = 0;

private:
  std::string _commandName;
  int _num_req_args;
};
